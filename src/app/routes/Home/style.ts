import styled from '@emotion/styled'

const Text = styled.div`
  font-size: 24px;
  font-weight: 700;
`

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
`

export { Text, Wrapper }
